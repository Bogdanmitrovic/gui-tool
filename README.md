Project configured to use JVM 21.

You can run it from IntelliJ IDEA (.idea and .run folders uploaded) or by executing the following commands in the root folder of the repository:

linux/macOS:
```
./gradlew build

./gradlew run
```

Windows:
```
gradlew build

gradlew run
```
